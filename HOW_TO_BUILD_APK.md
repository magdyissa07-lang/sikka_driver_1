# تحويل الكود لملف APK تقدر تنزّله على موبايلك — بدون أي برمجة

## الخطوات

1. **اعمل حساب على GitHub** (مجاني): https://github.com/signup

2. **اعمل مستودع (Repository) جديد فاضي**، ادوسي على زر "+" أعلى الصفحة ثم
   "New repository". سمّيه مثلاً `sikka-rider`. سيبه Public أو Private براحتك،
   وادوسي "Create repository".

3. **ارفع مجلد المشروع كامل** (`sikka_rider`) للمستودع:
   - في صفحة المستودع الفاضي، هتلاقي رابط "uploading an existing file" —
     ادوسي عليه واسحب كل ملفات ومجلدات `sikka_rider` (لازم تفضل بنفس هيكلها:
     `lib/`, `pubspec.yaml`, `codemagic.yaml`, ...) وادوسي "Commit changes".

4. **اعمل حساب على Codemagic** (مجاني لعدد ساعات بناء شهريًا كفاية جدًا
   لتجربتك): https://codemagic.io/signup — سجّل دخول بحساب الـ GitHub بتاعك
   نفسه عشان يبقى الربط تلقائي.

5. **اربط المشروع**: من لوحة Codemagic ادوسي "Add application"، اختار
   المستودع `sikka-rider` اللي رفعته، واختار "Flutter App" كنوع المشروع.

6. Codemagic هيكتشف ملف `codemagic.yaml` الموجود في المشروع تلقائيًا (فيه
   خطوة بناء APK جاهزة). ادوسي **"Start new build"** واختار الـworkflow اسمه
   `android-apk`.

7. استنى كام دقيقة (البناء الأول بياخد حوالي 5-10 دقايق). لما يخلص، هتلاقي
   في صفحة النتيجة قسم **Artifacts** فيه ملف بامتداد `.apk` — ده التطبيق
   جاهز.

8. **نزّل الملف على موبايلك مباشرة** (Codemagic بيديك رابط تحميل تقدر تفتحه
   من متصفح الموبايل نفسه)، أو حمّله على الكمبيوتر وابعته لموبايلك عبر
   واتساب/بلوتوث.

9. لما تفتح الملف على أندرويد، هيقولك "المصدر غير معروف" — ده طبيعي لأي
   تطبيق مش نازل من Google Play. ادوسي "تثبيت على أي حال" / "Install anyway"
   ووافق على السماح بالتثبيت من هذا المصدر.

10. التطبيق هيفتح ويشتغل فورًا — **من غير أي سيرفر**، لأنه شغال في وضع
    التجربة (Demo Mode) زي ما هو موضح في `README.md`.

## ملاحظة أمان
متسيبش المستودع Public لو حاطط فيه بعد كدة مفاتيح حقيقية (زي مفاتيح
Paymob أو Google Maps) — خليه Private، وربط Codemagic بمستودع Private
شغال بنفس الطريقة بالظبط.
